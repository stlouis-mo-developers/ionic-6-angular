import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic 6 Angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
